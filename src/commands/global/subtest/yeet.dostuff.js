'use strict';

/** @type {import("../../../types").Command} */
module.exports = {
    name: "dostuff",
    description: "do stuff idk",
    execute: interaction => {
        interaction.reply("e");
    }
}